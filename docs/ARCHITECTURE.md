# Architecture

One Field Kit on one external drive, separate platform applications. Windows is completed and field-validated first; macOS Intel and Apple Silicon follow with the same shared UX/source concepts where practical. Root Windows launchers load application/installer PowerShell source in memory rather than asking the technician to directly run a downloaded PS1.
