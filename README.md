# T3CHNRD Digital Field Kit

Windows-first rebuild with root-level portable/install entry points, restored original UI/UX, and exact recoverable diagnostic scripts. Scripts whose authoritative original body cannot be recovered remain disabled instead of being rewritten.
